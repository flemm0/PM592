```{r}
library(arrow)
library(tidyverse)
library(skimr)

rm(list = ls())

nhanes <- arrow::read_parquet("./data/nhanes.parquet")
```

```{r}
skim(nhanes)
```

```{r}
nhanes %>%
  na.omit() %>%
  mutate(gender = ifelse(gender == 2, 0, gender))
```

